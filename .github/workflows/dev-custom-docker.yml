name: Dev Custom Docker

on:
  push:
    branches:
      - dev_custom
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: dev-custom-docker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: ubuntu-24.04
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4

      - name: Log in to GHCR
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve image tags
        id: image
        shell: bash
        run: |
          set -euo pipefail
          image="$(printf 'ghcr.io/%s' "${GITHUB_REPOSITORY}" | tr '[:upper:]' '[:lower:]')"
          short_sha="${GITHUB_SHA::12}"
          {
            echo "name=${image}"
            echo "tags<<EOF"
            echo "${image}:dev_custom"
            echo "${image}:dev_custom-${short_sha}"
            echo "EOF"
          } >> "${GITHUB_OUTPUT}"

      - name: Build and push Docker image
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7.2.0
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.image.outputs.tags }}
          cache-from: type=gha,scope=dev-custom-docker
          cache-to: type=gha,mode=max,scope=dev-custom-docker
          provenance: false
          build-args: |
            OPENCLAW_DOCKER_APT_UPGRADE=0

      - name: Print image
        run: |
          echo "Pushed:"
          echo "${{ steps.image.outputs.tags }}"
